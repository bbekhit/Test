-- json response can be seen in network tab

-- killall node or pkill -8000 node// to kill running ports

-- engines in package.json when eslint gives error not supported version(jonas-node)
"engines":{
"node":">=10.0.0"
}
