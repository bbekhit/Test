// time complexity: how to analayze the runtime of algorithm as the size
// of input increases

// space complexity: how much additional memory do we need to allocate
// to run the code the algorithm

Object;
insertion: o(1);
removal: o(1);
access: o(1);
search: o(n);

Arrays;
insertion: o(1); //at the begining o(n)
removal: o(1); //at the begining o(n)
access: o(1);
search: o(n);

Arrays;
push - pop; //o(1)
shift;
unshift;
concat;
slice;
splice;
forEach, map, filter;
sort; //O(n * log n)
